package Model;

public enum TicketStatus {

	PENDING(1), 
	APPROVED(2),
	DENIED(3)
	
	 private final int value;
	 
    private TicketStatus(int value) {
        this.value = value;
    }

    public int getValue() {
        return value;
    }
}
